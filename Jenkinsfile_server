pipeline {
    agent any

    stages {
        stage('Deploy') {
            steps {
                sh '''
                pwd
                ls -l

                npm install
                pm2 restart devops-app || pm2 start app.js --name devops-app
                '''
            }
        }
    }
}

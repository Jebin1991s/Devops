pipeline {
    agent any

    stages {
        stage('Deploy') {
            steps {
                sh '''
                cd /home/ec2-user/devops-app
                git pull origin main
                npm install
                pm2 restart devops-app || pm2 start app.js --name devops-app
                '''
            }
        }
    }
}
